<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nodemailer App</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Times New Roman', Times, serif;
        }

        body {
            width: 100%;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background-image: url('https://programminginsider.com/wp-content/uploads/2021/09/email.gif');
            background-position: right;
            background-repeat: no-repeat;
            background-size: cover;
        }

        .result{
            background-color: #fff;
            padding: 20px;
            border-radius: 20px;
            width: 70%;
            text-align: center;   
        }

        a.goBtn {
            padding: 10px;
            display: inline-block;
            margin-top: 20px;
            color: #000;
            text-decoration: none;
            background-color: rgb(238, 131, 131);
            font-size: 18px;
            font-weight: 900;
            border: none;
            border-radius: 10px;
            box-shadow: 2px 2px 5px rgb(0, 0, 0);
            cursor: pointer;
            transition: all 0.4s;
        }

        a.goBtn:hover {
            background-color: rgb(177, 239, 85);
            transform: translateY(-8px);
            color: rgb(255, 255, 255);
        }

        .nodemailerapp {
            max-width: 70%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: left;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 6px 6px 12px rgba(0, 0, 0, 0.8);
        }

        .nodemailerapp > h2 {
            margin: 8px 0 10px 0;
            padding: 5px 0;
            background-color: rgb(237, 237, 14);
            width: 100%;
            text-align: center;
        }

        p {
            font-style: italic;
            font-weight: 900;
            text-align: center;
            padding: 20px;
        }

        p.error{
            background-color: rgba(255,0,0,.1);
            color: rgb(255,0,0);
            width: 100%;
            margin-bottom:10px;
            border-radius:10px;
        }
        p.success{
            background-color: rgba(0,255,0,.1);
            color: rgb(21, 167, 21);
            width: 100%;
            margin-bottom:10px;
            border-radius:10px;
        }

        .container {
            width: 100%;
            padding: 0 10px;
            border-radius: 10px;
        }

        .email,
        .subject,
        .emailtext {
            padding: 10px;
        }

        .email>input, 
        .subject>input{
            background-color: rgba(60, 60, 60,.1);
            width: 100%;
            padding: 10px;
            font-size: 16px;
            font-weight: 600;
            border-radius: 8px;
            outline: none;
            border: none;
        }

        .emailtext>textarea {
            background-color: rgba(60, 60, 60,0.1);
            width: 100%;
            padding: 10px;
            font-size: 16px;
            font-weight: 600;
            border-radius: 8px;
            outline: none;
            resize: none;
            border: none;
        }

        .btnCenter {
            text-align: center;
            padding: 10px;
        }

        button {
            padding: 10px;
            width: 200px;
            background-color: rgb(238, 131, 131);
            font-size: 18px;
            font-weight: 900;
            border: none;
            border-radius: 10px;
            box-shadow: 2px 2px 5px rgb(0, 0, 0);
            cursor: pointer;
            transition: all 0.4s;
        }

        button:hover {
            background-color: rgb(177, 239, 85);
            transform: translateY(-8px);
            color: rgb(255, 255, 255);
        }

        .status-message {
            background-color: rgb(196, 249, 117);
        }
    </style>
</head>

<body>
    <%if(result){ %>
       <%if(result?.success){ %>
            <section class="result">
                <p class="success"><%=result?.message%></p>
                <a class="goBtn" href="/">Go Back</a>
            </section>
       <% } %>
       <%if(!result?.success){ %>
        <section class="result">
            <p class="error"><%=result?.error%></p>
            <a class="goBtn" href="/">Go Back</a>
        </section>
   <% } %>
    <% } %>
    
    <%if(!result){ %>
        <section class="nodemailerapp">
            <h2>NodeMailer App</h2>
            <p>Send emails easily using NodemailerApp. Just fill in the form and click "Send Email" to get started</p>
            <form action="/send-email" method="POST" id="myForm" class="container">
                <div class="email">
                    <input required type="email" placeholder="Enter recipient email" name="userEmail" id="userEmail">
                </div>
                <div class="subject">
                    <input required type="text" placeholder="Enter Subject" name="userSubject" id="userSubject">
                </div>
                <div class="emailtext">
                    <textarea required name="userText" id="userText" cols="30" rows="10" placeholder="Enter Your Message"></textarea>
                </div>
                <div class="btnCenter">
                    <button type="submit" class="sendBtn" id="sendBtn">Send Email</button>
                </div>
            </form>
            <p>&copy; 2024 NodemailerApp. All RightReserved</p>
        </section>
   <% } %>
</body>

</html>